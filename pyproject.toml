[project]
name = "galaxy-archive-embedding-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "confluent-kafka>=2.14.2",
    "flagembedding>=1.4.0",
    "kiwipiepy>=0.23.2",
    "logging>=0.4.9.6",
    "numpy>=2.4.6",
]
