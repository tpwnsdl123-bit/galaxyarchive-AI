name: "galaxyarchive"
# for local dev
services:
  galaxy-ai-worker:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: "galaxy-ai-worker"

    restart: unless-stopped

    environment:
      KAFKA_BOOTSTRAP_SERVERS: "galaxy-kafka:29092"
      ROOT_MODEL_DIR: /app/models
      EMBEDDING_MODEL_NAME: "Qwen/Qwen3-Embedding-0.6B"
    volumes:
      - ./models:/app/models

    networks:
      - galaxy-network

networks:
  galaxy-network:
    name: galaxy-network
    external: true
